# Copy to .env and fill in real values.
# .env is gitignored — never commit real values.

AWS_REGION=

# Infrastructure identifiers (required)
ECR_REGISTRY=
ECR_NAMESPACE=
COSIGN_KEY=

# Optional
ENVIRONMENT=
REQUIRE_SIGNATURE=
LORE_TARGETARCH=
AUTH_TARGETARCH=
