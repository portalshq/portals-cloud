# Local env — contains secrets, never commit
.env.compose.local
*.local

# Build outputs
dist/
node_modules/
.turbo/
target/

# Compose runtime
.compose-data/

# Lore TLS certs (private keys — never commit)
infra/lore/certs/

# OS
.DS_Store
