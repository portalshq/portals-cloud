# @portalshq/capability-text-image-delivery

25thChapter's content model, generalized. This package should consume
`@portalshq/capability-narrative-engine-adapter` for chapter/branch state rather
than reimplementing narrative logic — it owns presentation/delivery, not
narrative structure.
