{
  "name": "@portalshq/runtime-core",
  "version": "0.0.6",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/portalshq/portals-cloud"
  },
  "description": "Session orchestration (control plane) + real-time fan-out gateway (data plane). Now instrumented with billing metering hooks.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "publish": "node ../../scripts/publish-workspace.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0",
    "vitest": "^3.2.7"
  }
}
