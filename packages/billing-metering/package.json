{
  "name": "@portalshq/billing-metering",
  "version": "0.0.3",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/portalshq/portals-cloud"
  },
  "description": "Thin wrapper over the OpenMeter HTTP API. The single instrumentation surface for all billable events flowing through the platform. runtime-core calls this; nothing else does.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@portalshq/contracts": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0"
  }
}
