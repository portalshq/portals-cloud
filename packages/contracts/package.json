{
  "name": "@portals/contracts",
  "version": "0.0.1",
  "description": "Capability contract types and channel manifest schema. The single source of truth every other package validates against.",
  "main": "src/index.ts",
  "dependencies": {
    "zod": "^3.23.0"
  }
}
